# Design register

Every decision the Design chapter makes where the Blueprint's fixed decisions left a choice, with the alternative rejected and the reason, and every assumption the chapter rests on. Entries are added as the pages are written and are numbered in the order taken. A reader who disagrees with a decision can find the page it takes effect on; a reader who wants to know what the design assumes can read the second list without the chapter.

## Decisions

| id | decision | rejected alternative and reason | page |
|---|---|---|---|
| D1 | The vOSTD is the `ostd` crate under a Cargo feature `kernelet`, built with `--no-default-features`; the kernel crates gain a matching feature that drops the machine-facing components and turns on `forbid(unsafe_code)`. | A second package over the same source. It would let one Cargo build hold both variants, which nothing needs once the two images are separate builds, and it doubles the manifests. | [Builds and images](../blueprint/design/builds-and-images.md#two-builds) |
| D2 | Control and data cross the ELF boundary only through a service table and an entry table of `extern "C"` function pointers, exchanged at the image's entry point, plus shared pages; the host finds the entry point through `e_entry` and the entry table at a fixed offset. | Resolving host symbols by relocation at image-combine time, as a Linux module does. Direct calls instead of indirect ones, but it needs a merged symbol namespace or a relocation processor in the trusted base, and spreads the import surface over the symbol table instead of one struct checked by a source hash. | [Builds and images](../blueprint/design/builds-and-images.md#entry) |
| D3 | *Revised by [Design for Linux](../blueprint/linux-mode/builds-and-images.md#sharing).* The kernelet image is position-independent, and the host loads each instance at an offset of its choosing in the shared kernel address space; one physical copy of a kind's read-only segments serves every instance, and program-counter-relative addressing selects the running instance's private writable data. Formerly: the image was linked at fixed addresses, every kind at the same addresses, inside two top-level page-table entries of a per-kernelet kernel page table. | Fixed addresses with a private kernel page table per kernelet: impossible on a host whose kernel half is shared by every process, and the kernelet-versus-kernelet separation it appeared to buy was not real, since the host's linear map is shared into every kernelet's page table. | [Builds and images](../blueprint/design/builds-and-images.md#window) |
| D4 | Kernelet images are embedded in the host image with `include_bytes!` and their segments are copied into fresh frames at registration. | Bootloader modules (`MemoryRegionType::Module`), which allow replacing a kind without relinking; kept as the extension. Mapping the embedded bytes in place, which needs page alignment of the byte string and page-aligned segment offsets. | [Builds and images](../blueprint/design/builds-and-images.md) |
| D5 | The endovisor's hooks are a trait object per kernelet, called synchronously on the kernelet's own task inside the service call. | A request queue served by an endovisor thread: keeps host Linux code off the kernelet's stack, but turns every device register access into a cross-task round trip with a wakeup (9.4 µs where a function call is 35 cycles, measured on the booted prototype). | [Control half](../blueprint/design/kernelet-api-control.md) |
| D6 | Device models live in the endovisor, in the safe-Rust kernel proper; OSTD knows a device only as a register window and an interrupt line. | Device models inside OSTD: smaller crossing, but puts Linux functionality (files, sockets) into the `unsafe`-bearing framework. | [Control half](../blueprint/design/kernelet-api-control.md) |
| D7 | The service half identifies the caller from a per-CPU slot the host scheduler writes, not from an argument on every call. | A context argument on every call: lets one function serve several kernelets on one stack, which nothing needs, and puts an unforgeable-by-convention pointer on every crossing. | [Service half](../blueprint/design/kernelet-api-service.md) |
| D8 | Per-task records shared read-write carry only the kernelet's preemption count and a read-only mirror of the host's flags; the service-call depth, the flags themselves, the recorded page-table root and the saved user state are host-private per-task state. | Keeping the depth and flags on the shared page: a kernelet could then hold its depth at one and never be killed. Service calls for the preemption count: on every lock, too many crossings. | [Service half](../blueprint/design/kernelet-api-service.md) |
| D9 | Deferred work (virtual interrupts, idle ticks, grants) is fetched by worker tasks through a parking service call; the host never enters the entry table except at task start. | Host-driven upcalls into the kernelet for each job: re-entrancy, and the service-call depth stops being a single bit. | [Service half](../blueprint/design/kernelet-api-service.md) |
| D10 | *Superseded by D58.* Formerly: the host bootstraps `KW_HEAP` at creation and the kernelet maps later runs by writing entries into reserved level-2 frames of its own grant. | | [Memory](../blueprint/design/virtualizing-ostd/memory.md) |
| D11 | *On the Linux host, replaced by D117: handlers run in an upcall on the virtual CPU's own carrier, and there are no worker tasks. Unchanged for the Asterinas host.* Formerly: One worker task per virtual CPU, pinned to it, so the kernel proper's per-CPU tick and bottom-half code runs on the virtual CPU it believes it is on. | One worker per kernelet: fewer stacks, but per-CPU kernel code would run on the wrong virtual CPU. | [Service half](../blueprint/design/kernelet-api-service.md) |
| D12 | Hooks do not sleep; host I/O behind a virtual device is done by an endovisor device thread per device, woken by the notify hook, charged to the kernelet by adoption, through the CPU slot. | Sleeping hooks: a driver holds a spin lock across a register write, so a sleeping hook would sleep with preemption disabled. Doing I/O inline: same problem. | [Control half](../blueprint/design/kernelet-api-control.md) |
| D13 | *Superseded by D58.* Frame metadata lives in a sparse metadata window indexed by physical address, in host frames the host maps per grain, not at the head of the run. | | [Memory](../blueprint/design/virtualizing-ostd/memory.md) |
| D14 | *Withdrawn.* Page-table nodes are addressed through the window like every other frame; with the host bootstrapping the first run (D10), nothing needs a linear-map exception. | The earlier form of this decision, a linear-map exception for nodes, rested on a bootstrap the host now does. | [Memory](../blueprint/design/virtualizing-ostd/memory.md) |
| D15 | *Revised for the Linux host only by D116 to D119: there the kernel proper's scheduler takes effect, on virtual CPUs that Linux schedules. Unchanged for the Asterinas host.* Formerly: The kernel proper's injected scheduler is inert inside a kernelet; the host schedules the kernelet's threads by their per-thread `nice` and virtual-CPU affinity, under the kernelet's quota. | Consulting the kernelet's scheduler as a second-level policy from the host's run queue: enters kernelet code from the scheduler's path with locks held, on a task that may not be the kernelet's, breaking the entry rule. | [Tasks](../blueprint/design/virtualizing-ostd/tasks.md) |
| D16 | Kernel-mode preemption exists in the host build only for returns from the trap path into kernelet text at service-call depth zero, honoring the task record's preemption count; `preempt_switch` is the tree's own switch protocol (`before_switching_to`, `context_switch`, `after_switching_to`) entered with interrupts disabled and without the sleep assertion. | Preempting host code too: breaks the host's lock discipline. A switch path of its own that skips the tree's protocol: the resumed side would not run `after_switching_to`. | [Tasks](../blueprint/design/virtualizing-ostd/tasks.md) |
| D17 | On an involuntary switch away from kernelet code the host saves the task's user FS and GS bases and FPU state, and restores them on the way back. | Forbidding kernel-mode preemption while user state is live in the CPU: every system call becomes a preemption-off region. | [Tasks](../blueprint/design/virtualizing-ostd/tasks.md) |
| D18 | `InterruptLevel` is virtualized to report the level of the job a worker is delivering. | Leaving it identical, always task context: the bottom-half dispatch is `unreachable!` at level zero, the CPU-time statistics and process clocks charge nothing, and the softirq guard would run pending softirqs at the wrong time. | [Interrupts and time](../blueprint/design/virtualizing-ostd/interrupts-and-time.md) |
| D19 | Ticks are counted at `TIMER_FREQ` on busy virtual CPUs and posted at a policy rate `idle_tick_hz` (default 100 Hz) to an idle kernelet's virtual CPU 0; a one-shot `timer_arm` is the extension toward tickless idle. | 1 kHz per virtual CPU per kernelet regardless: a thousand wakeups per second per CPU for an idle sandbox. No idle ticks at all: a sleeping process's timer would never fire in an idle kernelet. | [Interrupts and time](../blueprint/design/virtualizing-ostd/interrupts-and-time.md) |
| D20 | The ring transition is one service call (`user_run`); the `execute` loop, the pre-run hook and the kernel-event check stay in the kernelet. | Moving the loop into the host: the kernel's `UserModeHooks` would cross the boundary as closures. | [User mode](../blueprint/design/virtualizing-ostd/user-mode.md) |
| D21 | A kernel-mode page fault in a user copy is resolved by the host jumping to the image's exception-table recovery address with the fault recorded in the task record; the kernelet's copy routine calls its own injected fault handler and retries. | The host calling the kernelet's injected handler from its fault path: enters kernelet code from the host's fault path. | [User mode](../blueprint/design/virtualizing-ostd/user-mode.md) |
| D22 | A page fault in kernelet code with no exception-table entry kills the kernelet (`KernelFault`), not the machine. | Treating it as a host panic, as the host kernel does for its own faults: a kernelet bug would reset the machine. | [User mode](../blueprint/design/virtualizing-ostd/user-mode.md) |
| D23 | Virtual devices are enumerated from the kernel command line the endovisor composes, reusing the kernel proper's MMIO bus probe with `cfg` lines for the interrupt and PCI steps. | A kernelet-specific bus that reads `BootArgs` directly: a second probe to maintain. | [Devices](../blueprint/design/virtualizing-ostd/devices.md) |
| D24 | A descriptor outside the grant fails the request with the device's error status; it never kills the kernelet. | Killing on a bad descriptor: turns a tenant driver bug into a denial of service the tenant can trigger at will. | [Devices](../blueprint/design/virtualizing-ostd/devices.md) |
| D25 | The first version's network device is user-space backed through the kernelet runtime. | An in-kernel backend over a host tap or bridge: the host kernel has neither (A7). | [Devices](../blueprint/design/virtualizing-ostd/devices.md) |
| D26 | `main` runs on the boot task, which exits when `main` returns. | Ending the kernelet when `main` returns: it returns before the init process runs. Parking the boot task: a 512 KiB stack held for the sandbox's life for nothing. | [The rest](../blueprint/design/virtualizing-ostd/the-rest.md) |
| D27 | Init exiting ends the kernelet as `Exited(status)` through one `cfg` line, not as a panic. | Keeping the host's panic: the runtime could not tell a clean shutdown from a crash. | [The rest](../blueprint/design/virtualizing-ostd/the-rest.md) |
| D28 | Every caught panic is reported to the host through the `oops` service call, from OSTD's `catch_unwind`, with the message stashed at the handler's entry. | Counting oopses only in the kernel proper: the budget could not be a host policy. | [The rest](../blueprint/design/virtualizing-ostd/the-rest.md) |
| D29 | The grant table is a host-written page in `KW_SHARED` that the kernelet only reads; it lists each run's physical base and length. | Having the kernelet keep its own copy from a call it must answer: an upcall. | [Memory](../blueprint/design/virtualizing-ostd/memory.md) |
| D30 | Memory is granted in physically contiguous, 2 MiB-aligned runs of grains; `grains_request` can ask for a contiguous run. | Single grains: caps a kernelet's largest contiguous allocation under 2 MiB, which the kernel proper's own file systems exceed. | [Memory](../blueprint/design/virtualizing-ostd/memory.md) |
| D31 | `TaskOptions` gains `cpu_affinity` and `nice` builders and matching setters in vOSTD; the kernel proper forwards a thread's affinity and `nice` through one `cfg` line each. | Leaving the hints out: the kernel proper's per-CPU daemons would land on arbitrary virtual CPUs. Adding the builders to the host build too: the host's threads already carry the attributes. | [Tasks](../blueprint/design/virtualizing-ostd/tasks.md) |
| D32 | RCU grace periods are tracked per virtual CPU with an extended quiescent state for idle virtual CPUs. | Per-task quiescence: a period would wait for tasks parked for the life of the sandbox. Per-virtual-CPU without the extended state: a period never completes while any virtual CPU is idle. | [Tasks](../blueprint/design/virtualizing-ostd/tasks.md) |
| D33 | `GsBase::load` and `save` are virtualized to direct `IA32_KERNEL_GS_BASE` MSR accesses, since the tree's `swapgs` bracket is safe only with interrupts really disabled. | Leaving them identical under a preemption guard: a host interrupt between the two `swapgs` would run on the tenant's GS base, a host compromise. | [Virtualizing OSTD](../blueprint/design/virtualizing-ostd/index.md) |
| D34 | A dying kernelet's tasks are terminated at service-call depth zero regardless of their preemption count. | Honoring the count until a budget expires: delays reclamation for no one's benefit. | [Faults](../blueprint/design/faults-and-reclamation.md) |
| D35 | Termination discards a task's stack rather than unwinding it. | Unwinding: runs the tenant's destructors on the host's behalf and cannot be done beneath a trap frame. | [Faults](../blueprint/design/faults-and-reclamation.md) |
| D36 | `destroy` never waits for a pin or an adopted thread; it returns `Zombie` and is retried. | Waiting inside `destroy`: blocks the runtime's process for as long as a device thread's host I/O takes. | [Faults](../blueprint/design/faults-and-reclamation.md) |
| D37 | All communication between a kernelet and the host or another kernelet is vsock over the ordinary device path. | A dedicated cross-kernelet channel with its own service calls: a second mechanism to secure and to learn. | [Channels](../blueprint/design/channels.md) |
| D38 | The vsock switch copies payloads in the first version; a frame-move extension is stated with its preconditions and left out. | Frame moves now: changes the ownership model and A1 before the copy path is measured. | [Channels](../blueprint/design/channels.md) |
| D39 | Host-side vsock endpoints are stream descriptors from the endovisor ABI, not a new `AF_VSOCK` family in the host kernel. | A host socket family: more surface than the runtime needs. | [Channels](../blueprint/design/channels.md) |
| D40 | The endovisor is a module of the kernel crate in safe Rust over the control half, not a module of OSTD. | Inside OSTD: it holds Linux functionality (files, threads) OSTD does not have. | [The endovisor](../blueprint/design/endovisor.md) |
| D41 | The endovisor ABI is one character device with typed `ioctl`s and pollable sandbox and endpoint descriptors, in the shape of `/dev/kvm`. | New system calls: nothing a runtime knows how to drive. | [The endovisor](../blueprint/design/endovisor.md) |
| D42 | Closing the last sandbox descriptor kills, waits for and destroys the kernelet. | Sandboxes outliving their runtime by default: a crashed runtime leaks them. | [The endovisor](../blueprint/design/endovisor.md) |
| D43 | A guest agent, as the kernelet's init, drives processes inside over vsock. | No agent: the runtime cannot start a process inside a kernel it cannot enter. | [The kernelet runtime](../blueprint/design/kernelet-runtime.md) |
| D44 | The root file system is an ext2 image built from the bundle, cached by content, attached read-only under an overlay inside; a `virtiofs` server backend in the endovisor is the planned replacement, the client existing on the tree. | The `virtiofs` backend first: the largest device model on the critical path of the first sandbox. | [The kernelet runtime](../blueprint/design/kernelet-runtime.md) |
| D45 | The first version's network is a user-space NAT in the runtime. | An in-kernel backend: the host has no tap or bridge (A7). | [The kernelet runtime](../blueprint/design/kernelet-runtime.md) |
| D46 | *Withdrawn.* Replaced by D66: a busy virtual CPU's ticks are consumed on the interrupted task, so the tick callbacks are identical code. | | [Interrupts and time](../blueprint/design/virtualizing-ostd/interrupts-and-time.md) |
| D47 | The user context passed to `user_run` must lie on the calling task's kernel stack; the host checks it. | A host-private per-task copy of the context, copied in and out on every round trip: two copies of about 160 bytes on the hottest path. | [User mode](../blueprint/design/virtualizing-ostd/user-mode.md) |
| D48 | vOSTD initializes the kernel proper's `PANIC_ON_OOPS` to `false`, enabling the oops path the host keeps off. | Leaving it off: every tenant-kernel panic ends the sandbox and the oops budget counts nothing. | [The rest](../blueprint/design/virtualizing-ostd/the-rest.md) |
| D49 | The `logger` component stays in the kernelet, and vOSTD sends every filtered record to `log_write` before the injected logger. | Making `logger` host-only: takes `print`/`println` out of the prelude and the kernel's log off the tenant's console. | [The rest](../blueprint/design/virtualizing-ostd/the-rest.md) |
| D50 | The notify hook validates chains and records them into an inbox preallocated at attach; the device thread pins and copies. | Copying in the hook: unbounded work under the driver's spin lock. Allocating in the hook: an allocation that can fail where nothing can sleep. | [Devices](../blueprint/design/virtualizing-ostd/devices.md) |
| D51 | The vsock switch rewrites `buf_alloc` in forwarded headers to the smaller of the advertised credit and the policy cap, and resets a sender that exceeds it. | Back-pressure by not draining the sender's transmit queue: stalls every connection on that device. | [Channels](../blueprint/design/channels.md) |
| D52 | `KERNELET_CREATE` only configures a sandbox; `KERNELET_START` calls `Kernelet::create` and `start` from the gathered configuration. | Creating the kernelet at `CREATE` and adding devices afterward: needs an `add_device` that mutates a created kernelet's boot arguments. | [The endovisor](../blueprint/design/endovisor.md) |
| D53 | Possession of a sandbox descriptor is the capability to operate on it; no per-call owner check. | A `Uid` check per `ioctl`: a credential lookup on every call, and a descriptor passed to another process becomes unusable. | [The endovisor](../blueprint/design/endovisor.md) |
| D54 | The sandbox boots and the agent handshake completes at OCI `create`; `start` only starts the container process. | Booting at `start`: `create` cannot fail on a bad environment, `kill` in `created` has nothing to signal, and the boot hides inside `start`. | [The kernelet runtime](../blueprint/design/kernelet-runtime.md) |
| D55 | Grains are zeroed at grant, before they are published. | Scrubbing at destroy: 2 MiB per grain on the destroy path, and the tenant's data stays in the frame while the kernelet is a zombie. | [Faults, termination, and reclamation](../blueprint/design/faults-and-reclamation.md) |
| D56 | `mark_dying` is a compare-and-swap, a store to the info page and a signal; the reaper task walks the task table, sends the reschedule interrupts, runs `on_dying`, clears jobs and deadlines, frees terminated tasks' stacks, and moves the kernelet to `Exited`. | Walking the task table in the detecting context: takes a lock inside the double-fault handler on a CPU that may hold it. Running `on_dying` there: a Linux-side hook inside an interrupt handler. | [Faults, termination, and reclamation](../blueprint/design/faults-and-reclamation.md) |
| D57 | The switch away from a kernelet task loads the host kernel root into CR3. | Flushing at destroy: a freed grant frame stays some CPU's live page-table root until then. | [Faults, termination, and reclamation](../blueprint/design/faults-and-reclamation.md) |
| D58 | *Revised by [Design for Linux](../blueprint/linux-mode/virtualizing-ostd/memory.md).* `paddr_to_vaddr(pa)` is the **host's own linear-map base** plus `pa`, with the base supplied in `BootArgs` and held in the instance's data; frame metadata keeps a per-instance sparse window `KW_META` at a base the host chooses. Formerly: both were sparse windows at fixed addresses inside the kernelet's own two top-level entries. | A per-instance physical window in the shared address space: it would reserve address space proportional to the machine's physical memory for every kernelet. Sharing the host's metadata array as well: a miscomputed address would then corrupt another tenant's or the host's frame records instead of meeting an unmapped page. | [Memory](../blueprint/design/virtualizing-ostd/memory.md) |
| D59 | Each kernelet task holds the `Arc<VmSpace>` it last activated, so a `VmSpace` is dropped only when no task holds it and `pt_root_unregister` never fails. | A pending list of page tables whose unregister was refused, retried at every activation: bookkeeping for a case the tree's own invariant prevents. | [Memory](../blueprint/design/virtualizing-ostd/memory.md) |
| D60 | A TLB flush batch above four operations is one `tlb_shootdown` of everything non-Global under the root; a batch of one is one call. | One crossing and one interrupt round trip per operation: up to 32 of each per batch. | [Memory](../blueprint/design/virtualizing-ostd/memory.md) |
| D61 | A kernelet's tasks are threads of the host kernel proper, created through the `spawn_task` hook; `KerneletTaskBody::run` is OSTD's trampoline. | OSTD creating bare host `Task`s: the host's class scheduler enqueues only tasks that are `Thread`s. Teaching the scheduler a second kind of task: more kernel-proper code for less. | [Control half](../blueprint/design/kernelet-api-control.md) |
| D62 | The CPU quota is a throttle in OSTD driven by the host tick's per-kernelet charge; the weight is a per-thread `nice`. | A scheduler group with a weight and bandwidth control: the host has none, and building one is a scheduler project. | [Control half](../blueprint/design/kernelet-api-control.md) |
| D63 | Hook-bearing service calls run the hook on a per-CPU host stack with the host's preemption disabled, under `catch_unwind`; a panic in a hook kills the kernelet with `HostHookPanicked`. | Running hooks on the kernelet task's stack: the endovisor's depth becomes a tenant-reachable double fault when the reserve is wrong, and a panic in the endovisor halts the machine. | [Service half](../blueprint/design/kernelet-api-service.md) |
| D64 | `KerneletConfig::max_tasks` bounds a kernelet's threads; their stacks are charged to its host-overhead account. | An unbounded task count: 512 KiB of host memory per thread at the tenant's discretion. | [Control half](../blueprint/design/kernelet-api-control.md) |
| D65 | No service call returns a value through a pointer; results come back in registers, virtual-CPU sets are a `u64` (`MAX_VCPUS = 64`), and the three read-only pointers are read with fallible copies. | Output pointers with a range check: a store through a pointer the kernelet chose faults when the kernelet unmaps the page. | [Service half](../blueprint/design/kernelet-api-service.md) |
| D66 | A busy virtual CPU's ticks are counted by the host into a shared per-virtual-CPU record and consumed by the kernelet's own task at its next tick point, in task context; only an idle kernelet's ticks are worker jobs. | Delivering every tick on the worker: two host context switches per millisecond per busy virtual CPU, and two `cfg` lines so that the callbacks can charge the interrupted thread. | [Interrupts and time](../blueprint/design/virtualizing-ostd/interrupts-and-time.md) |
| D67 | vOSTD spawns no idle threads: the boot CPU's idle loop becomes a thread that waits on init, the other idle loops are not spawned, two `cfg` lines. | Idle tasks parked and woken edge-triggered when a virtual CPU goes idle: a stack per virtual CPU and a wake protocol in the host's park and exit paths. | [Tasks](../blueprint/design/virtualizing-ostd/tasks.md) |
| D68 | Vsock reachability between sandboxes is deny by default; a runtime that holds both sandbox descriptors opens a pair with `KERNELET_VSOCK_POLICY`. | Allow within one user: on a node where containerd owns every sandbox, no policy at all. | [The endovisor](../blueprint/design/endovisor.md) |
| D69 | The kernelet build of OSTD is named **vOSTD**, virtual OSTD; the host build is plain **OSTD**, with "host build" said only where the build itself is the subject. | Symmetric names ("OSTD (host build)" and "OSTD (kernelet build)", or "host OSTD" and "kernelet OSTD"): they read as two peers, when one is the framework as it exists and the other a variant that presents its API. | [Boundaries and trust](../blueprint/design/principles.md) |
| D70 | The second version's devices are lending devices: rings in the kernelet's memory read through `guest_memory`, entries that lend the kernelet's own frames, host drivers that take those frames as DMA targets. | Keeping virtio and changing only the backend: the descriptor walk, the device thread and the copy stay. Rings mapped read-write on both sides: below the isolation floor. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) |
| D71 | The host, the device and a receiving kernelet act only on bytes the host copied out of a submit entry and validated; network and channel headers travel inside the entry, a receiver gets the switch's copy of a channel header, and a device that writes its own header in-band (virtio-net) gets a host-owned descriptor for it, so a lent frame is never the first descriptor of a receive chain. | Parsing headers or lengths in the tenant's frames: the tenant can change them after the host's check, and a forged in-band receive header desynchronizes the host driver. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) |
| D72 | Lending is enforced twice: in the kernelet a reference the driver holds until completion (a `BioSegment`'s `DmaStream`; the frame a network driver keeps), and in the host a 16-bit lend count in the owner-and-lend word (slot 16, generation 32, count 16), advanced by one compare-and-swap with the owner check and rolled back if a later segment fails; a lent grain is never moved or freed, the count is in destroy's drain list, and completion takes it down even for a dying kernelet. | Either alone: rests on the other side's correctness. A separate check and count: a move or destroy can interleave. A 32-bit count: does not fit the word. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) |
| D73 | Submission happens in the notify hook, at most 32 entries per call (8 on a channel device), non-sleeping, under the device lock taken with interrupts disabled, into a `submit_nowait` of the host driver whose completion callbacks run with no driver lock held; a channel device's complete-ring cursor lives in the switch's table under the switch lock; a device thread exists only for a backend that must sleep. Until the tree's block layer has `submit_nowait` (A14), block pays one host-thread wakeup per batch. | A thread per device on the fast path: the wakeup the model removes. An unbounded pass over a 256-entry ring: up to a millisecond under a spin lock. A lock without interrupts disabled, a completion under the driver's lock, or another kernelet's device lock from a hook: a deadlock. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) |
| D74 | Interrupt suppression is one polling bit the kernelet writes, with a `SeqCst` fence on both sides of the two-flag hand-off, the OR of two conditions: more than four initiated requests in flight, polled from the kernel proper's own per-device threads; and a receive interrupt not yet drained. | Virtio's event index: more state for the host to validate. No fence: a missed completion is a hang until the next one. Counting posted receive frames toward the threshold: a network device would poll forever. Two clearing rules on one bit: each clears the other's polling. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) |
| D75 | Network receive is zero-copy only with a per-kernelet receive queue; a shared queue costs one host copy and is stated as such, and on a single-MAC cloud host demultiplexes by IP. Transmit is refused unless the EtherType is IPv4, IPv6 or ARP without a VLAN tag, the source addresses are the sandbox's, ARP and neighbor-discovery fields carry them, and router advertisements and redirects are absent. | Claiming zero-copy receive on a shared queue: the device does not know the destination before the packet lands. Source-address checks alone: ARP and neighbor-cache poisoning, rogue router advertisements. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) |
| D76 | Inter-kernelet transfer copies once, frame to frame, in the sender's notify hook, into a 16 KiB frame the receiver posted for that connection (named by the vsock four-tuple in the entry's header); the receiver advertises one frame of credit per posted frame, four per connection, and the sender charges a whole frame per data `TX`; the switch writes the validated header at the head of the frame; header-only packets land in a control pool the receiver posts, with one slot reserved per live connection and direction and connection requests under a per-sender quota, and a host queue of 64 headers per device while the pool is empty; posted frames and cursors live in the switch's tables under the switch's lock, the payload copy outside it; credit advances at re-post; a torn-down connection's posted frames are completed with an error; rings are unpinned at `on_dying`. Moving whole grains stays the Channels extension. | Two copies through device threads (D38). A host queue for payloads: two copies again and the head-of-line stall D51 rejects. Byte credit against frame-granular delivery: a sender within its credit is refused. A host-assigned connection number: no completion carries it. No reserve in the control pool: a third kernelet's connection requests can drop a live connection's credit update, which the tree never retries. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) |
| D77 | On bare metal a kernelet's grains are mapped into the host's IOMMU domain at grant, not per request. This widens the isolation floor's "lent frames while the host holds them" to every grain, on the device side only: an IOMMU mapping is not a CPU mapping and gives the tenant no reach. | Per-request IOMMU map and unmap with deferred invalidation: keeps the floor literally, at a sub-microsecond cost per request and an invalidation queue to bound. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) |
| D78 | *Revised by D93 and D95.* Formerly: in Linux mode the tenant's processes are Linux processes, and the kernelet owns their memory through a fault handler on their virtual memory areas. | A kernelet-built address space entered from a kernel thread: Linux never returns a kernel thread to user mode and offers no way to substitute a page table on the way out. | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md) |
| D79 | *Revised by D91.* Formerly: in Linux mode the tenant's system calls reach the kernelet through a per-task hook added by a patch, and **Linux mode requires that patch**. Syscall User Dispatch yields no unpatched alternative: Linux clears it in `copy_process()` and in `begin_new_exec()`, so only a tenant's first thread is ever intercepted and a sandbox is a process tree; the tenant can also store to the selector byte or jump to the stub's own instruction. It is kept as a *measured* lower bound on interception that goes through user space: at least +885 ns per call in the test guest, against a hook that adds nothing measurable to a 44 ns floor. | Syscall User Dispatch as a second tier: withdrawn, because it cannot run a process tree. Seccomp user notification and `ptrace(PTRACE_SYSEMU)`, measured on the build host at 5,721 ns and 8,221 ns against that machine's 1,887 ns for dispatch: each puts the answer in another process. | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md) |
| D80 | *Revised again by D120: five exports (`mm_alloc` added) and one helper, `kernelet_switch_mm()`.* *Revised by D89: four exports, `kernel_clone` added; `get_vm_area` and `init_mm` withdrawn.* Formerly: Linux mode requires of the kernel what a module cannot do for itself. The one it cannot start without is an exported `set_memory_rox`, because `vmap()` strips the execute permission, no permission setter is exported, and `execmem_alloc()` is not exported; `set_memory_rox` rather than `set_memory_x` because the latter leaves the mapping writable and executable. `set_memory_rw` is its mandatory partner, because `set_memory_rox` also clears the write bit on the frames' direct-map alias and they cannot be returned to Linux until it is restored. A complete Linux mode wants three more: `set_memory_ro`, for the relocated read-only data, which is hardening; and `get_vm_area` with `init_mm`, without which the frame-metadata region cannot be sparse and invariant I3's last hardware check is lost — `apply_to_page_range`, which is the only exported way to populate such a range, takes the address space to operate on and the one it needs is not exported. Populating the metadata region needs `apply_to_page_range`, which is already exported; nothing else that would do the job is. Two things once on this list are not needed: physically contiguous runs above the page allocator's 4 MiB maximum, since `alloc_contig_range` is exported, though the wrapper that searches for a range is not, so the endovisor does that search itself; and a tenant thread's thread-pointer register, which can be written by setting the task's saved field. | Loading each instance through Linux's own module loader: it gives every instance its own copy of the text, in a region of 1520 MB shared by all modules on the machine. | [The endovisor as a Linux module](../blueprint/linux-mode/endovisor.md) |

## Assumptions

| id | assumption | where it matters | standing |
|---|---|---|---|
| A1 | A kernelet's memory only grows: grains are granted, never reclaimed from a running kernelet; memory returns when the kernelet exits. A balloon is the extension. | [Control half](../blueprint/design/kernelet-api-control.md) | design choice, recorded |
| A2 | *Withdrawn by [Design for Linux](../blueprint/linux-mode/builds-and-images.md#sharing).* Formerly: the refill cost of the window's non-Global translations after a CR3 write is acceptable without PCIDs. With one shared kernel address space there are no per-kernelet non-Global window mappings to refill. | [Builds and images](../blueprint/design/builds-and-images.md) | superseded |
| A3 | A 64 KiB stack reserve is enough for the deepest host path a service call takes. | [Service half](../blueprint/design/kernelet-api-service.md) | *estimated*; to be measured on the host paths the device models use |
| A4 | The kernelet image's private writable state is under 128 KiB before per-CPU replicas. | [Builds and images](../blueprint/design/builds-and-images.md) | *estimated* from the tree's debug image (`.data` 27,464, `.bss` 23,944, `.cpu_local` 1,977 bytes). **Rests on the superseded D3**: under the revised scheme the per-instance cost also carries the relocated read-only data and, on Linux, one small-page translation per page of image, and it no longer carries per-kernelet top-level page-table entries. Not re-derived |
| A5 | The host kernel's file and socket objects can be driven by a kernel thread without a process context. | [Control half](../blueprint/design/kernelet-api-control.md) | **[unverified]**; the fallback is a borrowed process context on the device thread |
| A6 | A kernel-stack overflow in kernelet code is caught by a double-fault handler on its own interrupt-stack-table stack, at service-call depth zero, in a state from which the kernelet can be killed and the CPU switched away. | [Service half](../blueprint/design/kernelet-api-service.md) | **[unverified]** |
| A7 | A host-side software network interface registered with the host's network stack could serve as an in-kernel network backend, saving the user-space backend's two wakeups per frame. | [Devices](../blueprint/design/virtualizing-ostd/devices.md) | **[unverified]**; the host has no tap device or bridge today |
| A8 | The host can switch tasks from the interrupt-return path (`preempt_switch`) with every invariant of OSTD's trap and switch code intact. | [Tasks](../blueprint/design/virtualizing-ostd/tasks.md) | **[unverified]** |
| A9 | Per-virtual-CPU RCU quiescence with an extended quiescent state is sound under every path by which a task can leave a virtual CPU. | [Tasks](../blueprint/design/virtualizing-ostd/tasks.md) | **[unverified]** |
| A10 | *Withdrawn.* Replaced by D66: ticks are consumed on the interrupted task, so the callbacks need no argument. | [Interrupts and time](../blueprint/design/virtualizing-ostd/interrupts-and-time.md) | withdrawn |
| A11 | User-space NAT throughput for the network is acceptable for the targeted workloads. | [The kernelet runtime](../blueprint/design/kernelet-runtime.md) | **[unverified]**; A7 is the alternative |
| A12 | Every host top half and bottom half on the tree is correct with a kernelet task as `Task::current()`, which `user_run` makes the case for an interrupt landing in user mode; `Task::as_thread` returns `None` for such a task and the tick statistics take that path. | [User mode](../blueprint/design/virtualizing-ostd/user-mode.md) | **[unverified]**; the fallback is to switch to a per-CPU host task inside `user_run` before dispatching, one context switch per interrupt |
| A13 | *Withdrawn by [Design for Linux](../blueprint/linux-mode/builds-and-images.md#sharing).* Formerly: the host's physical address range fits the physical window's 512 GiB. There is no physical window to outgrow; what is now bounded is the address space each instance's metadata window reserves, which is the physical span it may be granted divided by 64. | [Builds and images](../blueprint/design/builds-and-images.md) | superseded |
| A14 | The host's block layer and drivers gain a non-sleeping, non-allocating `submit_nowait` over frames they did not allocate whose completion callbacks run with no driver lock held, a `BioSegment` over a lent physical range without remapping, an asynchronous NVMe driver, request timeouts, a receive-queue reset that reclaims posted buffers, a host-owned descriptor for a device's in-band receive header, a common buffer type for a host header followed by lent frames, a segment-taking transmit and a lent-frame receive path in virtio-net and the NIC drivers, and code that maps a kernelet's grains into the host's IOMMU domain at grant. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) | **[unverified]**; on the tree the block layer takes a sleeping lock and wakes a per-device thread, virtio-blk allocates and spins, NVMe issues one command at a time and sleeps, no driver times requests out, and DMA mappings are per object |
| A15 | A VM exit that returns to a user-space monitor costs 2–5 µs and one the kernel part of the hypervisor handles 0.5–2 µs on current hardware, scaled from Rizzo et al.'s 2013 figures of 3.7 µs and 1.2 µs. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) | The lower half is now **measured**, on the machine that produced the book's other figures: an exit and resume handled inside the kernel cost 3,002 cycles, 792 ns, with four different exit causes landing within three percent of each other ([Alternatives considered](../blueprint/linux-mode/alternatives.md)). The user-space figure remains **[unverified]**. Note that [Goals](../blueprint/overview/goals.md) states 5,155 cycles for a KVM exit and entry on the same machine; the two are not obviously measuring the same thing and the gap should be resolved |
| A16 | Per-kernelet receive-queue steering (a virtual function, a filtered queue, or one virtio-net device per kernelet on a cloud host) is available where tier 2 network receive is wanted. | [Zero-copy I/O](../blueprint/design/zero-copy-io.md) | deployment assumption |
| D84 | In Linux mode a kernelet task runs on a per-task kernelet stack that the system-call hook switches to on entry and away from on return, as Linux already does for hardware-interrupt handlers. Linux's own kernel stack is 16 KiB against the 512 KiB the design gives a kernelet task, and a quarter of the 64 KiB of headroom A3 reserves for the host path alone. The current task is found through a per-processor pointer rather than through the stack, and the scheduler saves only the stack pointer, so the task may sleep on the switched stack. | Running on Linux's own stack, which is a thirty-second of what the design reserves. | [Tasks](../blueprint/linux-mode/virtualizing-ostd/tasks.md) |
| D85 | *Superseded by D89.* Formerly: Linux mode adopts the **conservative restoration** of [Alternatives considered](../blueprint/linux-mode/alternatives.md): one gate in the generic entry layer rather than a hook per architecture entry; the kernelet as a binary-format handler, with `clone` declined to Linux and the child attached by the inheritance `copy_process` already performs; the kernelet's own page table kept as a model the hardware never walks; a die notifier and text revocation for containment and forced termination; a seat for per-processor soundness; control-group membership for accounting; and redirect-not-block for signals and traps. It asks Linux for less than D79 and D80 together ask today and delivers more: three exports, no boot setting, no build option. | The four other whole modes. The supervisor alias is adopted with it, gated on one measurement (A23). Per-instance loading as a genuine module is rejected on density, 56 sandboxes per machine; guest ring 0 restores all three properties and is rejected because it gives up kernel-mode execution, adds a hypervisor and taxes every tenant memory access; carriers are deferred as a layer on top of this one. | [Alternatives considered](../blueprint/linux-mode/alternatives.md) |
| D86 | Frame metadata is found by a two-level index over a coarse physical section, not by scaling a raw physical address. The region is then sized by policy rather than by the machine's physical span, which is what A22 measures. Applies to **both** hosts, and both functions are internal to OSTD, so no public interface moves. | The flat index, which makes the region's address span proportional to the machine and caps a host at about 2,000 kernelets per TiB. | [Memory](../blueprint/linux-mode/virtualizing-ostd/memory.md) |
| D87 | The kernelet image's exception table is encoded as pairs of **self-relative offsets**, as Linux encodes its own, not as pairs of absolute addresses. On the tree it is absolute, which under D3 makes it address-bearing, puts it on the wrong side of the build audit's own rule, and would fail audit item 8. Self-relative entries are shareable, identical in every instance, and are the precondition for a host searching a kernelet's table. Applies to **both** hosts. | Moving `.ex_table` to the per-instance region, which costs a copy and a relocation pass per instance and leaves the section unshareable. | [Builds and images](../blueprint/design/builds-and-images.md#window) |
| D88 | *Retired for the Linux host by D116: with a carrier per virtual CPU, per-CPU data belongs to the carrier and nothing is leased.* Formerly: A kernelet's per-processor data is selected by a **seat**, a lease a task acquires on entering kernelet code and releases on leaving, and `CpuId` names that lease rather than a processor. A kernelet has as many seats as virtual CPUs; race freedom comes from possession rather than from pinning or from a preemption count, so no build option of the host's is load-bearing. A seat also carries the service-call depth the design already keeps, the accounting scope, and a processor-time accumulator that can refuse an acquisition over quota. Adopted for Linux mode as part of D85, where the alternative is unsound; argued to be the better abstraction on **both** hosts, and not adopted for Asterinas mode here, because that would change `CpuId`'s meaning in a chapter this exploration did not review. | Deriving the virtual CPU from the processor, with a migration hold and a per-replica lock: sound, but it rests on host guards that a kernel built without preemption does not provide. Keeping the virtual-CPU selector as it is: unsound on Linux, since a tenant can have more tasks inside serviced calls than the kernelet has virtual CPUs. | [Tasks](../blueprint/linux-mode/virtualizing-ostd/tasks.md#vcpus) |
| A25 | *Withdrawn with D88: there are no seats on the Linux host.* Formerly: A seat's cost is an uncontended acquire and release per crossing, and the concurrency cap it implies — a tenant's in-kernel parallelism is limited to its virtual-CPU count — is acceptable. | [Tasks](../blueprint/linux-mode/virtualizing-ostd/tasks.md#vcpus) | **[unverified]**; the cap is a thing a tenant can observe, and it is a change in kind from a per-crossing overhead |
| D83 | *Withdrawn by [Alternatives considered](../blueprint/linux-mode/alternatives.md).* The legacy virtual system-call page calls `secure_computing()` before it invokes anything, and seccomp's filter input carries the instruction pointer, so a short filter closes it per sandbox with no patch and no boot setting. Formerly: Linux mode requires of the operator, beyond the module and the exports, that the kernel be booted with the legacy virtual system-call page disabled. It is emulated inside the page-fault handler and calls three of Linux's own system calls without passing dispatch, seccomp, ptrace or the kernelet hook, and its mode is a boot-time global the endovisor cannot vary per tenant. | Unmapping it per tenant, which the interface does not allow. | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md) |
| D82 | *Kept; completed by D95, which replaces the address-to-frame map with the model.* On a host that enables the processor's supervisor-access check — Linux does, Asterinas does not — the kernel proper reaches its tenant's memory through **vOSTD's own linear-map alias of the frames it granted**, never through the tenant's virtual address. The change is inside OSTD's copy routines, so the kernel proper's source does not move. *Measured* (Experiment 6): a bare kernel-mode read of a tenant address oopses on a present, mapped page; the same read through the supplier's alias succeeds with no bracketing at all; and bracketing the direct read succeeds for a good address but still oopses for a bad one, while the same bad read *recovers* when the faulting instruction carries a fixup entry — which works only because the test code is a module, whose table Linux searches, and a kernelet is not a module. So bracketing cannot deliver the *fallible* contract. **The rule needs an address-to-frame map, and two things about it are open.** A miss is structural, since a tenant's address space also holds the runtime's stub, Linux's virtual system-call pages and whatever Linux populated itself, and how often that happens is unmeasured. Worse, an entry that outlives its mapping does not miss but **resolves**, silently, against a frame that may already have gone back to the host and on to another kernelet — a hazard the alias rule introduces, since the hardware would otherwise have refused the access. Closing that is a **precondition of the rule**: it needs the sequence protocol Linux ships for keeping a second translation structure coherent with an address space, read-begin before the lookup and read-retry before the commit, all of which is exported. Two constraints follow: the notifier machinery is a build option Linux mode must require, and its invalidation callback may run where it must not sleep, so the map's lock must be a non-sleeping one and no crossing may happen while it is held. **D85 deletes all of this**: if the kernelet keeps its own page table as the model and Linux's tables are the cache, there is no second structure to keep coherent, no protocol to run, and no build option to require. | Every access as a service call: far more expensive than the evidence requires. Bracketing each access inside the kernelet, which the hardware permits since a kernelet runs in kernel mode, but which does not deliver the fallible contract. Registering the kernelet's exception table with Linux, which no exported interface allows. | [Memory](../blueprint/linux-mode/virtualizing-ostd/memory.md#copies) |
| D81 | *Superseded by D98, D99 and D93.* *Revised by [Alternatives considered](../blueprint/linux-mode/alternatives.md), and it was wrong in its framing when written.* Fault containment is bounded at service-call depth zero on **both** hosts, so what this entry recorded as a Linux deficiency was in part the design's own bound attributed to the host; and a module can catch a kernel-mode fault after all, through an exported notifier. What survives is the residue at depth one, which neither host can contain. Formerly: in Linux mode, invariant I7 (termination) does not hold, and the tenant's process lifecycle is not designed. Linux cannot stop a task running in kernel mode, so a runaway kernelet is an unkillable task and a destroy that never completes; the substitute is a cooperative check at each service-call boundary, with a deadline and a watchdog, which needs the kernelet to be working well enough to notice. Fault containment is weakened separately: a fault in kernelet code is a Linux oops rather than a contained kill, and nothing a module can do turns that into "end this kernelet and reclaim". No patch is proposed for it, because a fixup for a bug buys nothing and the containment wanted would mean unwinding whatever the faulting task held. The per-CPU replica selector also has no Linux counterpart, since `disable_preempt` increments a count only an Asterinas host honors, and on the patched path the kernel proper runs on the tenant's unpinned task. And the hook's shape cannot express `fork`, `execve` or a return from a signal handler, since only Linux can create a Linux task. | Forcing termination from the host, which the Asterinas-mode design assumes and Linux does not offer. | [Faults](../blueprint/linux-mode/faults-and-reclamation.md) |
| A17 | A small kernel patch adding a per-task system-call hook could be carried by an operator, or eventually upstreamed if reframed as an in-kernel dispatch target for Syscall User Dispatch. | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md) | **[unverified]**; as written it would probably be rejected upstream, and the chapter says so |
| A18 | Linux's block layer's existing asynchronous submission path can stand in for the non-sleeping submission the zero-copy design asks of the host's drivers. | [The endovisor as a Linux module](../blueprint/linux-mode/endovisor.md) | **[unverified]**; the shape carries over, the measured argument has not been rechecked |
| A19 | A kernelet image can run on a host whose own build enforces type-checked indirect branches. | [Builds and images](../blueprint/linux-mode/builds-and-images.md#audit), [Builds and images](../blueprint/design/builds-and-images.md) | **[unverified]**, and narrower than this entry first claimed — narrow enough that it was demoted out of the chapter's conclusion, because it is a hardening option rather than a property of Linux: it needs the kernel built with a compiler that x86-64 distributions do not use for it, and it can be turned off at boot with `cfi=off`. The plain hardware check is answered: `rustc -Z cf-protection=branch` emits the landing markers, *measured*. The type-checked form's hardest half is answered too, and by Linux itself: `init/Kconfig` makes `RUST` depend on `!CFI_CLANG \|\| HAVE_CFI_ICALL_NORMALIZE_INTEGERS_RUSTC` and selects integer normalization, and `arch/Kconfig` says the option "is necessary for using CFI with Rust", so Linux already requires its two compilers to compute the same type tag for the same prototype. What is open: the rewrite is driven by tables a build-time tool produces and a Rust image lacks; the functions that apply it are not exported; and it runs in **both** directions, since `cfi_rewrite_endbr()` poisons the landing marker at a function's entry (`arch/x86/kernel/alternative.c:1215`), so a kernelet's calls down into host service functions trap as well as the host's calls up |
| A21 | *Withdrawn: a tenant never calls Linux, so the restrictions of raw-frame mappings never reach it.* Formerly: a tenant's memory can be supplied through a fault handler on a virtual memory area without the restrictions that shape costing more than they are worth: pages inserted as raw frame numbers are refused by Linux's pinning interface, so the tenant loses direct file I/O, registered asynchronous-I/O buffers and remote direct memory access on them; they are outside the reverse map; and nothing calls back when Linux tears the mapping down. | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md) | **[unverified]**; each restriction is read from the source, and no measurement or prototype says whether a tenant can live with the set |
| A22 | *Superseded by D86.* Formerly: the address space the per-instance frame-metadata regions reserve, and not memory, is what bounds the number of kernelets a machine can hold, at one byte of address for every sixty-four of the physical span a kernelet's grains may touch — about 2,000 per TiB of span on four-level paging. That is the cost of the flat index D86 replaces; with a two-level index over a coarse physical section the region is sized by policy and the bound moves to about sixty thousand per TiB. A22 in turn superseded A13, which bounded the withdrawn physical window. | [Design for Linux](../blueprint/linux-mode/virtualizing-ostd/memory.md), [Memory](../blueprint/design/virtualizing-ostd/memory.md) | *arithmetic* over the region sizes in Linux's documentation; no machine has been filled with kernelets. Applies to **both** hosts, since neither bounds a kernelet's physical slice today |
| A23 | *Withdrawn by D108, on measurement.* Formerly: the supervisor alias costs little enough in translation-buffer pressure to be worth its hardening trade. The access itself is *measured* at 0.92 cycles against 1.08 for an ordinary kernel address and 38 for the host's own bracketed user access. | [Alternatives considered](../blueprint/linux-mode/alternatives.md) | **[unverified]**, and this is the gate on adopting it. A second translation is taken per touched page, since the same physical page is reached through two virtual addresses; the harness that measured the second-level translation cost of guest ring 0 can produce this number |
| A24 | *Resolved by measurement, see D95/D108: the walk costs about 10 ns per small copy, measured in a model; it takes no lock.* Formerly: the costs D85 puts on the path of every buffer-carrying system call — a page-table walk, plus a cursor lock and a replica lock on the same path — are worth what they buy. The walk is the certain cost and the locks are a workload-dependent tail; the supervisor alias removes the walk. | [Alternatives considered](../blueprint/linux-mode/alternatives.md) | **[unverified]**, and the single most likely thing to kill the adopted design. No first-wave agent could see it, because each saw one cluster. Measurable on the existing prototype. The carriers design reduces it, by dropping the number of cursors held at once from every tenant task to the number of virtual CPUs |
| A20 | *Standing now: partly verified, see the prototype page.* The stack switch of D84 survives what depends on the stack. | [Tasks](../blueprint/linux-mode/virtualizing-ostd/tasks.md) | **[unverified]**, and the hazard is worse than "backtraces stop at the switch": a guard-page overflow on a stack Linux does not know about is not diagnosed as a stack overflow at all, and ends in `panic("Machine halted.")`. The kernel's stack-validation tooling and its guard-page reporter must both be told about the frame |
| D89 | The final Linux design is one chapter, [Design for Linux](../blueprint/linux-mode/index.md), and it supersedes D85's list of parts. What it asks of Linux is the gate (D91), four exported symbols (`kernel_clone`, `set_memory_rox`, `set_memory_rw`, `set_memory_ro`), one module, and three host settings that the runtime checks: no panic on an oops, `debug.exception-trace` off, and `fs.suid_dumpable` at its default of 0. | The earlier ledger of three exports with `clone` declined to Linux: it needed the kernel proper to know its `clone` was serviced elsewhere. | [The endovisor](../blueprint/linux-mode/endovisor.md#patch) |
| D90 | *Revised by D116: the Linux task is a virtual CPU's carrier and runs whichever tenant thread the kernelet scheduled.* Formerly: On Linux, `user_run` is a real call that enters user mode and returns: the tenant's thread is a Linux task, the kernelet task's frames wait on a kernelet stack while Linux's stack empties, and `UserMode::execute` keeps its contract. | Servicing calls on a separate kernel thread: address-space work must run on the task whose address space it is, and a handoff costs microseconds. | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md) |
| D91 | The gate is two hooks in Linux's generic entry layer, keyed by an inherited per-task pointer and one syscall-work bit: a syscall hook after seccomp, and a resume hook at the top of the exit-to-user loop, before signal delivery. | A hook in one architecture's entry code, which covered one of x86-64's four entry instructions. | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md) |
| D92 | A tenant's exceptions other than page faults are taken off Linux's signal queue by the resume hook, with `dequeue_signal()`, before Linux delivers them; `SIGKILL` is left to Linux. | Blocking them (a forced signal is unblocked and reset, so a trap becomes a kill); catching them in a handler inside the tenant (needs a writable tenant stack and a trusted trampoline in tenant memory). | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md#exceptions) |
| D93 | *Revised by D116: a carrier carries a virtual CPU, not a task; they are cloned once, at start.* Formerly: Every kernelet task is carried for life by one Linux task, a **carrier**, cloned from the sandbox's root carrier with `kernel_clone()` and an in-kernel start function. Carriers inherit the gate, the seccomp filter, the control group, credentials and a blank address space. | Kernel threads (cannot enter user mode); letting Linux service the tenant's own `clone` (the kernel proper would have to know); the user-mode-helper interface (wrong control group, credentials and filter). | [Tasks](../blueprint/linux-mode/virtualizing-ostd/tasks.md#carriers) |
| D94 | The root carrier is made by a binary-format handler that claims the sandbox file: `exec` gives a blank address space, and Linux's own per-address-space module reference pins the endovisor. | An `ioctl` that converts the calling thread, which leaves the runtime's address space to be copied into every carrier. | [Tasks](../blueprint/linux-mode/virtualizing-ostd/tasks.md#root) |
| D95 | The kernelet's page table is the **model** and Linux's page table for a carrier is a **cache** of it, filled by the endovisor's fault handler, which walks the model itself and checks every frame against the grant; `tlb_shootdown` empties the cache. | Linux's address space as the truth with a kernelet-side address-to-frame map, which can go stale silently. | [Memory](../blueprint/linux-mode/virtualizing-ostd/memory.md#cache) |
| D96 | *Superseded by D120: one Linux address space per model, adopted by the virtual CPUs in turn.* Formerly: Each carrier caches in its own Linux address space; threads of one tenant process do not share Linux page tables. | Sharing, which needs knowledge OSTD's API does not give at task creation. | [Memory](../blueprint/linux-mode/virtualizing-ostd/memory.md#cache) |
| D97 | Grains are allocated on a carrier with `__GFP_ACCOUNT`, so a sandbox's memory is charged to its control group by membership. | Explicit charging by the endovisor. | [Memory](../blueprint/linux-mode/virtualizing-ostd/memory.md) |
| D98 | A carrier running kernelet code at depth 0 in a dying kernelet is removed by **eviction**: a timer callback on its processor points the interrupted frame's instruction pointer at the endovisor's exit stub, which leaves the kernelet through the ordinary stack switch. | Revoking the kernelet's text, which needs a machine-wide TLB flush and turns every termination into an oops; cooperative checks alone, which a loop without calls never reaches. | [Faults](../blueprint/linux-mode/faults-and-reclamation.md#eviction) |
| D99 | A fault in kernelet text at depth 0 is contained by a die notifier that redirects the carrier to the same exit stub, at the price of one printed oops, a tainted kernel and the operator requirement. | A patch teaching Linux's fixup search about kernelets: in this design no kernelet instruction is expected to fault, so the path is for bugs only. | [Faults](../blueprint/linux-mode/faults-and-reclamation.md#fault) |
| D100 | Kernelet-stack exhaustion is prevented by compiler-placed function-entry checks against a 16 KiB reserve, not caught. The reserve is for the Linux code that can run on top of an interrupted kernelet frame (the scheduler, the exception path). | Guard pages alone: an overflow on a stack Linux does not know escalates to a double fault and halts the machine. | [Faults](../blueprint/linux-mode/faults-and-reclamation.md#stack) |
| D101 | *Kept, reworded by D117: the timer is a virtual CPU's, and the tick comes from the per-processor watch timer only while a carrier runs.* Formerly: An idle kernelet arms no Linux timer; a seat's timer runs only while the seat is held or a deadline is set, and one machine-wide timer updates the shared clock page. | A fixed tick per kernelet, whose idle cost grows with the number of sandboxes. | [Interrupts and time](../blueprint/linux-mode/virtualizing-ostd/interrupts-and-time.md) |
| D102 | Device backends run on device threads created by the root carrier with `vhost_task_create()`, against files and sockets the runtime opened and passed by descriptor. | Backend I/O inside the service call, which would sleep holding the driver's lock; plain kernel threads moved into the control group, to which Linux never charges kernel memory. | [Devices](../blueprint/linux-mode/virtualizing-ostd/devices.md) |
| D103 | A kernelet's log goes to a per-sandbox, rate-limited ring that the runtime reads, not to Linux's log. | `printk`, which lets a tenant write to the operator's console. | [The rest](../blueprint/linux-mode/virtualizing-ostd/the-rest.md) |
| D104 | *Withdrawn by D116: with OSTD's own task layer in the build the call needs no special meaning.* Formerly: `Task::yield_now()` from the boot context waits until the kernelet has no tasks, which is what the call means on a machine. Found by the prototype. | Returning at once, which makes the 100-line kernel power off before its task runs. | [The rest](../blueprint/linux-mode/virtualizing-ostd/the-rest.md) |
| D105 | A sandbox starts when a prepared process executes its sandbox file, an in-memory file the endovisor creates and hands out only as an exec descriptor, and is owned by a descriptor; the runtime sets the control group, credentials, namespaces and seccomp filter with Linux's own tools first. | A start `ioctl`; a file on disk carrying a secret token, which a same-user process could read. | [The endovisor](../blueprint/linux-mode/endovisor.md#abi) |
| D106 | On Linux the initial grant is made at start, not at create, so that it is charged to the sandbox's control group. | Granting at create, when no member of the group exists. | [Control half](../blueprint/linux-mode/kernelet-api-control.md) |
| D107 | On Linux processor limits are the control group's, not the kernelet configuration's. | The throttle the Asterinas host needs because its scheduler has no groups. | [Control half](../blueprint/linux-mode/kernelet-api-control.md) |
| D108 | The supervisor alias is not adopted. Measured in a model, it saves about 7 ns on a small copy and nothing on a large working set, against writing page-table entries Linux owns and opening a kernel window onto user pages. A translation cache in front of the walk is not adopted either. | Adopting it as D85 proposed, gated on A23. | [Memory](../blueprint/linux-mode/virtualizing-ostd/memory.md#copies) |
| D109 | On Linux a lent frame is handed to the block layer as a `bio` segment submitted with `REQ_NOWAIT`, and to a socket as a spliced page. | Growing a non-sleeping submission path, which the Asterinas host must (A14). | [Zero-copy I/O](../blueprint/linux-mode/zero-copy-io.md) |
| D110 | On Linux the first version's network backend is a TAP interface. | The user-space translator of D45, which exists because the Asterinas host has no such machinery. | [The kernelet runtime](../blueprint/linux-mode/kernelet-runtime.md) |
| D111 | Filling and flushing the cache are interlocked by a per-model reader-writer lock in the endovisor (fills shared, `tlb_shootdown` exclusive, around `unmap_mapping_range()` on the model's file), and vOSTD frees no frame or page-table node that an unmap removed until the flush has returned. Found in review. | A sequence counter that lets a fill detect a concurrent flush and undo itself, which leaves a moment in which a stale translation exists. | [Memory](../blueprint/linux-mode/virtualizing-ostd/memory.md#interlock) |
| D112 | Service calls run on the carrier's Linux stack, not on the kernelet stack: each service-table entry is a stub that switches stacks around the call. Found in review. | Running Linux's code on the kernelet stack behind a reserve, whose adequacy cannot be argued and whose failure halts the machine. | [Service half](../blueprint/linux-mode/kernelet-api-service.md#depth) |
| D113 | The area's fault handler tells the kernel proper about a fault only if the tenant took it in user mode; a kernel-mode access by Linux that misses in the model gets `VM_FAULT_SIGBUS`, so Linux's own fixup returns `-EFAULT`. The handler builds page protections itself and never copies bits from a model entry. Found in review. | Claiming every fault on the area, which hangs a carrier unkillably when Linux reads the user stack on the 32-bit fast entry or in vsyscall emulation; copying the leaf's bits, which lets a model set the global bit. | [Memory](../blueprint/linux-mode/virtualizing-ostd/memory.md#cache) |
| D114 | *The user-mode tick is absorbed by D117 (any virtual interrupt flags a carrier in user mode); the lifeline half stands.* Formerly: A thread in user mode is brought back for kernel events by a per-kernelet user-mode tick at the kernel proper's tick rate, which flags each carrier in user mode; OSTD has no "kick" call and none is added. Every carrier holds a lifeline, an open endovisor file whose release is the notice of its death. | A kick service, which OSTD's interface has no caller for; the memory area's `close` callback as death notice, which carriers that never enter user mode do not have. | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md#tick) |
| D115 | *Extended by D117 and D119: the same watch timer delivers the tick and enforces the two-strike grace.* Formerly: On a Linux that does not preempt kernel code, the endovisor reschedules kernelet code itself: a per-processor watch timer, armed by a carrier whenever it enters kernelet code, in hard-interrupt context, redirects a carrier that Linux has marked as due to reschedule through a yield stub that saves the interrupted registers, calls `cond_resched()` on the Linux stack, and resumes. Found in review. | Requiring a preemptible kernel of the operator; restoring the other host's "preemption held off too long" kill, which would kill a sandbox for being busy. | [Tasks](../blueprint/linux-mode/virtualizing-ostd/tasks.md) |
| D116 | On Linux a carrier carries a **virtual CPU**, not a task. A sandbox has one carrier per virtual CPU, cloned once at start; kernelet tasks are OSTD's own objects, with 256 KiB kernelet stacks from `kstack_alloc`, drawn from a per-sandbox pool that returns nothing to Linux before destroy (freeing `vmalloc` memory flushes every processor's TLB), switched by OSTD's own context switch and chosen by whatever scheduler the kernel proper injects. Scheduling has two levels: Linux's group scheduler decides when the virtual CPUs run, by the sandbox's control group; the kernelet's scheduler decides, exactly, which of its tasks run on them. vOSTD virtualizes the processor (`vcpu_boot`, `vcpu_idle`, `vcpu_kick`) instead of the task. Revises D15 for the Linux host, D90 and D93; retires D88; withdraws D104. | A Linux task per kernelet task (scheduler inert, a Linux task and page table per tenant thread); hints through `nice` and real-time classes (not exact; real-time escapes the group's share); gating a task per thread by sleep and wake (a Linux switch per kernelet switch); a `sched_ext` BPF scheduler (the machine's one slot, fairness reimplemented); a hook in `__schedule()` (kernelet policy under the run-queue lock); gang scheduling (no such class, wasteful). | [Scheduling](../blueprint/linux-mode/virtualizing-ostd/scheduling.md) |
| D117 | Interrupts are virtual: bits in a per-virtual-CPU record shared with the endovisor (TICK from the watch timer, KICK, LINES for device lines), handled by an **upcall** on that virtual CPU's carrier. The record keeps OSTD's guard count (`guards`) and a virtual interrupt flag (`irq_off`), separately, as a machine does. In user mode the carrier is flagged and `user_run` returns 2; in kernelet code with virtual interrupts on, and at least 32 KiB of stack, the endovisor sets `irq_off` and redirects the interrupted frame to the image's upcall stub, which saves the interrupted state on the current task's stack so that the task may resume on any virtual CPU, and switches tasks at the end only if `guards` is zero; with interrupts off the bit waits. Nothing counts ticks; the record carries `stolen_ns`. A kick to a virtual CPU running kernelet code nudges its processor so the upcall is prompt. Replaces D11's worker tasks and job queues, and the user-mode tick of D114, on this host. | Worker tasks per virtual CPU (they are Linux tasks the kernelet's scheduler cannot see); running handlers in Linux's interrupt context (unstoppable, uncharged). | [Scheduling](../blueprint/linux-mode/virtualizing-ostd/scheduling.md#upcall) |
| D118 | OSTD prerequisites: for both hosts, one additive hook by which `halt_cpu()` learns the next timer expiry (D122); under the `kernelet` feature only, new bodies for the two guards' `Drop` (an end-of-critical-section preemption check, which fires only with interrupts on, and the mirror; today `DisabledPreemptGuard::drop` only decrements and `might_preempt()` is called only from user return, `halt_cpu` and new-task start), for the spin lock's slow path, for `halt_cpu`, and the new `virq_entry` stub with a preemption point at its end. The `Scheduler` and `LocalRunQueue` traits are unchanged. | Changing the scheduler traits (not needed: `update_current(Tick)` and `pick_next` already say everything); leaving kernel tasks non-preemptible (a compute-bound kernel thread would then defeat the kernelet's policy). | [Scheduling](../blueprint/linux-mode/virtualizing-ostd/scheduling.md) |
| D119 | Cooperation is by a **mirrored preemption count**: entering a critical section (the first guard, spin lock or interrupts-off guard) raises Linux's per-processor preemption count by exactly one, with the instruction Linux's own `preempt_disable()` uses, however deep the guards nest (Linux's count is an 8-bit field under its softirq and hardirq fields), so Linux does not deschedule a virtual CPU inside a critical section; when the outermost guard drops and Linux is waiting, vOSTD calls `vcpu_yield`. The deferral is bounded without reading the record: a carrier in kernelet code that has run 2 ms (*chosen*: twice the watch period) since its last real scheduling event (switched out by Linux, a sleeping service, user mode, a forced yield) is forced to yield where it stands, whether or not Linux is waiting (a raised count also holds off RCU); time so borrowed is charged to the sandbox. The endovisor restores Linux's count at every exit from kernelet code from its own record, not from anything the kernelet reports. A virtual CPU that spins on a lock calls `vcpu_on_spin`, at most once per tick, which becomes `yield_to()` a sibling carrier of the same sandbox without preempting its processor; with `NEXT_BUDDY` off (the default) this only gives the spinner's turn away. The watch timer runs on a processor exactly while a carrier is current there, armed from a preemption notifier on every switch-in. The design costs every task on a shared processor up to 2 ms of wake-up latency, stated as such; latency-critical host work gets a cpuset of its own. | No cooperation (lock-holder preemption: siblings spin for a whole Linux time slice); gang scheduling; a paravirtual "preempted" flag per virtual CPU without deferral (helps spinners, does not prevent the preemption); unbounded deferral (a tenant could hold a processor against its group's limit). | [Scheduling](../blueprint/linux-mode/virtualizing-ostd/scheduling.md#cooperative) |
| D120 | Each model has one Linux address space, created with `mm_alloc()` when the model is registered and adopted by a carrier, at `user_run`, when it is about to run a thread of that process. The patch gains one helper, `kernelet_switch_mm()`, modelled on `kthread_use_mm()` and `exec_mmap()` for a task that is not a kernel thread, which consumes a reference on the new address space and returns the old one, and which releases and acquires the concurrency ids of restartable sequences under the run-queue lock as `exec` does (found in review: without that, each adoption leaks one id and the processor eventually hangs); and one export, `mm_alloc`. Every carrier holds its own reference on the address space it has adopted. A model's area is created under a per-model mutex. Supersedes D96 and withdraws A26; revises D80. | A Linux address space per carrier with the area remapped at every switch (a full cache flush per switch); `kthread_use_mm()` (refuses user tasks); a Linux task per tenant thread (D93 as it stood). | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md#adopt) |
| D121 | Floating-point state is per tenant thread. OSTD's `FpuContext::save` and `load`, which the kernel proper already calls around a task switch and for signal frames, become the services `fpu_save` and `fpu_load`, which work through a staging buffer in the carrier record and never on kernelet memory in place: under `fpregs_lock()`, make the carrier's user registers live if they are not, then `xsave` to staging or `xrstor` from validated staging, with a recovery entry on the `xrstor`. Loadable components: x87, SSE, AVX, AVX-512; not PKRU, not dynamically enabled state. | Linux's exported guest-FPU swap (only in kernels built with virtualization support, and in Linux's own format); lazy switching by trapping (Linux owns the control register). | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md#fpu) |
| D122 | OSTD prerequisite: a hook by which the owner of a timer wheel reports its earliest expiry on this CPU, consulted by `halt_cpu()`, so that vOSTD can give `vcpu_idle` a deadline and an idle virtual CPU costs no timer interrupts; without it vOSTD idles to the next tick. OSTD has only a periodic tick, so there is no `timer_arm` service. | Idling to the next tick always (a wake-up per millisecond per idle virtual CPU, the cost D101 avoids); a one-shot timer service (no caller in OSTD). | [Interrupts and time](../blueprint/linux-mode/virtualizing-ostd/interrupts-and-time.md) |
| A26 | *Withdrawn by D120: the cost no longer exists.* Formerly: One Linux address space per carrier is affordable: a page touched by N threads of a tenant process costs N cache fills, and a flush visits N address spaces. | [Memory](../blueprint/linux-mode/virtualizing-ostd/memory.md#cache) | **[unverified]**; the largest performance cost the Linux design accepts, not measured |
| A27 | Eviction works: a timer callback may rewrite the interrupted instruction pointer of a carrier that is in kernelet text at depth 0, and have the interrupt return to the endovisor's exit stub. | [Faults](../blueprint/linux-mode/faults-and-reclamation.md#eviction) | *measured on the booted prototype* for one kernelet spinning in `loop {}` on a preemptible Linux 6.12: evicted in 8 runs of 8, within two 1 ms sweeps; not tried under load, with many carriers, or across kernelets |
| A28 | Function-entry stack checks cost one to two percent of kernel time. | [Faults](../blueprint/linux-mode/faults-and-reclamation.md#stack) | *estimated*; not measured on a kernelet |
| A29 | The yield stub works: an interrupted kernelet computation can be suspended from a timer callback, rescheduled through Linux's voluntary preemption point, and resumed intact. | [Tasks](../blueprint/linux-mode/virtualizing-ostd/tasks.md#yield) | *measured on the booted prototype*, Linux 6.12 with `preempt=none`: more than 800 yields in an eight-second computation, result bit-identical, a competitor's longest starvation cut from 4.3 s to 0.27 s; one kernelet, one seat |
| A30 | Raising Linux's per-processor preemption count from kernelet text, and restoring it at every exit from kernelet code, is sound: Linux neither warns nor misbehaves, and no path leaves the count wrong. | [Scheduling](../blueprint/linux-mode/virtualizing-ostd/scheduling.md#cooperative) | *measured on the booted prototype*: 0 involuntary preemptions in a critical section over 267 M lock acquisitions with the mirror on, 1,467 with it off, no count fault in any run once the mirror was made idempotent; one host configuration only |
| A31 | `kernelet_switch_mm()` is correct for a user task: references, the concurrency ids of restartable sequences, the multi-generation LRU's list and exit-time reference counts all stay consistent. | [User mode](../blueprint/linux-mode/virtualizing-ostd/user-mode.md#adopt) | *measured on the booted prototype*: 1,267 adoptions alternating one carrier between two models, with `SCHED_MM_CID` on, past the count at which the id leak would have hung the processor; the LRU-list and lazy-TLB points were found in review and are argued |
| A32 | The upcall works: an interrupted virtual CPU can be redirected to the image's stub, the kernelet can switch tasks inside it, and the interrupted task can resume later on another virtual CPU. | [Scheduling](../blueprint/linux-mode/virtualizing-ostd/scheduling.md#upcall) | *measured on the booted prototype*: 1,842 switches in a three-second run, tick-driven, legal under a reference model; the yield kernel's checksum bit-identical after 3,936 upcalls |
| A33 | Two ticks of grace are enough for OSTD's and the kernel proper's critical sections, so that the forced yield is rare. | [Scheduling](../blueprint/linux-mode/virtualizing-ostd/scheduling.md#cooperative) | *measured on the booted prototype* for the test kernels: 0 forced yields except against a deliberate 10 ms overstayer; **[unverified]** for the real kernel proper |
| A34 | Two-level scheduling costs about what Linux's own group scheduling costs: a switch between kernelet tasks and a wake-up-to-run are no slower than between Linux threads in a control group. | [Scheduling](../blueprint/linux-mode/virtualizing-ostd/scheduling.md) | *measured on the booted prototype*: 542 ns round trip between kernelet tasks, 411 ns wake-to-run on one virtual CPU, against 924 ns for a Linux `sched_yield` round trip on the same guest; the cross-CPU wake-up and the cross-process tenant switch are **[unverified]** |
