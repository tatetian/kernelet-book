# The kernelet API: service half

> **To be written.** Question 3. The service table OSTD (host build) hands a kernelet image at entry, the entry table it receives back, and the shared pages between them; every function with its signature, what it checks, and what a crossing costs.
