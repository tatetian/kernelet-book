# Builds and images

> **To be written.** How `ostd` is built twice and how the kernel proper becomes `aster-kernelet`; the kernelet image as an ELF of its own, its sections and fixed addresses, the boot image that combines it with the host image, the entry point, and the two tables exchanged there; the symbol-table audit.
