# The kernelet runtime

> **To be written.** Question 4, user side. An OCI runtime-spec runtime over the endovisor ABI: `config.json` to kernelet configuration, the root filesystem, networking, console and `exec`, the lifecycle operations, and driving it from containerd.
