# Virtualizing OSTD

> **To be written.** Question 2. The taxonomy: every essential public API of OSTD with its kind in the kernelet build (identical, virtualized, absent), how a virtualized item is implemented, the service-half API it relies on, and its cost; one page per resource follows.
