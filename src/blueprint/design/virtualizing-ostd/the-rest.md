# Boot, power, panic, and the rest

> **To be written.** `boot_info`, the command line, `power`, the panic handler and `catch_unwind`, `smp`, `bus`, and the `arch` surface: what is identical, what is synthetic, and what is absent.
