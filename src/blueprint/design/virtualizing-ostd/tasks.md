# Tasks, scheduling, and CPUs

> **To be written.** `Task` over host-owned tasks, spawning by entry index, the shared task block for preemption and dying state, waiting and waking by name, the scheduler hook, RCU, virtual CPUs and `cpu_local!`.
