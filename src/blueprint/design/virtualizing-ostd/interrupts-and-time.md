# Interrupts and time

> **To be written.** `IrqLine` as virtual lines delivered on the kernelet's worker task, bottom halves, timers and `Jiffies`, and why a kernelet never runs in interrupt context.
