# Devices

> **To be written.** `IoMem` as a register file over the endovisor's device models, virtio over function calls, the owner checks on descriptors, `DmaStream` and `DmaCoherent`, console and logging.
