# Memory

> **To be written.** Frames and grants, per-kernelet frame metadata, the heap window and `paddr_to_vaddr`, `VmSpace` and page tables over the kernelet's kernel page table, TLB shootdown, DMA objects.
