# The kernelet API: control half

> **To be written.** Question 1. The API OSTD (host build) provides so that the endovisor can register images, create kernelets, grant them memory and CPUs, attach devices, start, observe, kill and destroy them, and receive upcalls; as Rust items with their guarantees and costs.
