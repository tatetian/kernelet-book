# Crate layout, build and test harness

> **To be written.** The concrete crate graph (`aster-kernelet`, `kernelet-abi`, `endovisor`, the host kernel's manifest), the OSDK changes (linker script, `v0` mangling, the window sections), how the same source builds as the host kernel and as a kernelet, how a `#[ktest]` obtains a kernelet to run in, and the CI jobs that run the checks of [Enforcement](checks.md). Source material: the manifest trick and the kernel split in [Design](../design/facade/index.md), and item 18 of [what must be verified](../limitations/todos.md).
