# Architecture

This chapter is the whole system on a few pages: what runs where, what each layer guarantees and what enforces it, what the design deliberately does not do, and what is shared by every kernelet and therefore trusted. The [Design](../design/index.md) chapter then builds each layer in full; the one-figure version of this chapter is in the [Preface](../executive-summary.md#academia).
